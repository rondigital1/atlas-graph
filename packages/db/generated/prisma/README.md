# Generated Prisma Client Placeholder

This directory is the configured Prisma Client output target for AtlasGraph.

It is intentionally empty during `T-001`. Later tickets can populate it by running:

```bash
pnpm prisma:generate
```
